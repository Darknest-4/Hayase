node_modules

# Output
.output
.vercel
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
ideas.todo

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*.mkv
*.webm
*src/routes/test/**
dist/

# Host-specific compose overlay: which external network this machine puts the
# app on, and which services it runs. Not the same on two installs.
docker-compose.override.yml
