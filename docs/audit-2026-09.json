{
  "generatedAt": "2026-09-14T04:31:13Z",
  "commit": "1eff72f45aecb8d925a0c4f28f56997f6457c6b0",
  "summary": {
    "critical": 1,
    "high": 4,
    "medium": 9,
    "low": 8
  },
  "findings": [
    {
      "id": "YUME-AUDIT-0001",
      "severity": "CRITICAL",
      "category": "functional",
      "file": "apps/web/src/shared/state/store.js",
      "line": 120,
      "title": "The pre-namespace key migration needed room for the library twice and threw on a full quota",
      "impact": "A browser whose local library is large — the founder's account holds the whole catalogue — got a blank page and nothing else. ensureProfiles() is the first statement of App.init(), so QuotaExceededError killed the boot before the router ran: every asset returned 200, the server log was clean, and the site was unusable with no way to tell why.",
      "suggestedFix": "Rename the key by freeing the old value before writing the new one, and put it back if the write still fails, so the migration can never stop the application starting.",
      "effort": "S",
      "status": "fixed"
    },
    {
      "id": "YUME-AUDIT-0002",
      "severity": "HIGH",
      "category": "database",
      "file": "apps/api/src/integrations/anilist/sync.ts",
      "line": 187,
      "title": "11 703 anime carry no anilist_id, so the AniList enricher can never match them",
      "impact": "13 038 of 32 390 titles have no description. 11 703 of those are unreachable by design rather than by accident: upsertMedia() finds its row with SELECT ... WHERE anilist_id = $1 and returns false when there is none, and those rows were seeded from a MAL-keyed dump holding mal_id only. No number of enrichment runs will ever fill them, so a third of the catalogue shows an empty synopsis for ever.",
      "suggestedFix": "Give the enricher a MAL-keyed path — resolve anilist_id from mal_id via the AniList API once and store it — so a row seeded without an AniList id becomes reachable.",
      "effort": "M",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0003",
      "severity": "HIGH",
      "category": "functional",
      "file": "apps/api/src/app.ts",
      "line": 422,
      "title": "The SPA fallback answers a missing client module with index.html and a 200",
      "impact": "Any import that stops resolving — a file moved by a restructure, a typo in a path — is served as HTML with status 200 instead of 404. The browser refuses to run it as a module and the page goes blank, while every check an operator would make says the site is healthy: the JS URL returns 200, the server logs nothing. This is what made YUME-AUDIT-0001 take so long to find and it will do the same to the next one.",
      "suggestedFix": "Exclude the client asset directories from the SPA fallback so a missing file under /src, /css or /assets returns a real 404.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0004",
      "severity": "HIGH",
      "category": "security",
      "file": "apps/api/src/modules/catalogue/public-routes.ts",
      "line": 347,
      "title": "POST /v1/anime/resolve lets any signed-in account create catalogue rows at the global rate limit",
      "impact": "The route creates an anime stub for an AniList id it has not seen, and carries only the 300-requests-a-minute global limit — no WRITE_LIMIT and no per-account cap. One ordinary account can insert hundreds of thousands of rows a day into the anime table, which is the table this instance most needs to protect, and every one of them appears in search and in the sitemap.",
      "suggestedFix": "Put WRITE_LIMIT on the route and cap how many stubs one account may create per day.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0005",
      "severity": "HIGH",
      "category": "functional",
      "file": "infrastructure/docker/Dockerfile",
      "line": 8,
      "title": "docker build -t yume-test . fails: the restructure left no Dockerfile at the repository root",
      "impact": "The documented and habitual build command exits with \"failed to read dockerfile: open Dockerfile: no such file or directory\". CI passes because .github/workflows/check.yml names the file explicitly, so anyone building by hand — which is how a release gets made in a hurry — hits a failure CI cannot warn them about.",
      "suggestedFix": "Either restore a root Dockerfile or change every document and runbook to the -f infrastructure/docker/Dockerfile form, and say which one is canonical.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0006",
      "severity": "MEDIUM",
      "category": "security",
      "file": "apps/web/src/shared/api/yume.js",
      "line": 36,
      "title": "Access and refresh tokens live in localStorage, where script can read them",
      "impact": "There is no cookie anywhere in the API, so HttpOnly, Secure and SameSite are not merely unset — they are unavailable. Any script that runs on the origin can read a refresh token good for 30 days and use it from anywhere. The strict CSP (script-src 'self') is the only thing standing in front of it, which makes one injection into a same-origin file a full account takeover rather than a session-long nuisance.",
      "suggestedFix": "Move the refresh token to an HttpOnly, Secure, SameSite=Strict cookie and keep only the short-lived access token in memory.",
      "effort": "L",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0007",
      "severity": "MEDIUM",
      "category": "security",
      "file": "apps/web/src/shared/ui/charts.js",
      "line": 242,
      "title": "The donut legend interpolates its label into innerHTML without escaping",
      "impact": "Charts.donut() builds each legend row as an HTML string containing d.label and d.color. Today those are genre and format names from the catalogue, so exploiting it needs the anime.edit permission — but it is an unescaped HTML sink on a page an administrator opens, which makes it a privilege-escalation step rather than a harmless one, and nothing stops a future caller passing a username.",
      "suggestedFix": "Build the legend row from elements and textContent instead of an HTML string.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0008",
      "severity": "MEDIUM",
      "category": "database",
      "file": "apps/api/src/modules/system/diagnostics.ts",
      "line": 256,
      "title": "The metric-age probe sequentially scans every system_metrics partition",
      "impact": "SELECT max(created_at) FROM system_metrics has no index it can use — the only index is (metric, created_at DESC) — so the diagnostics page reads all three partitions in full. Measured on the live database: a parallel sequential scan over 231 944 rows, 2 689 buffers, 28 ms, and it grows with the retention window. pg_stat_user_tables records 34 198 sequential scans of the current partition.",
      "suggestedFix": "Add an index on created_at, or take the age from system_metrics_hourly, which is small and already ordered by hour.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0009",
      "severity": "MEDIUM",
      "category": "database",
      "file": "database/migrations/0026_mapping_conflicts.sql",
      "line": 24,
      "title": "343 unresolved mapping conflicts are recorded and shown nowhere",
      "impact": "0026 was written so that external-id collisions could be reviewed instead of losing a batch, and the recording half works — 368 rows, 343 unresolved. The reviewing half does not exist: no route reads mapping_conflicts and no screen shows it, so the candidate duplicate pairs it was built to surface sit in a table nobody opens. The table comment calls it a review queue; there is no queue.",
      "suggestedFix": "Add a read route and a catalogue-section list for unresolved conflicts, with the resolve action the resolved_at column was added for.",
      "effort": "M",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0010",
      "severity": "MEDIUM",
      "category": "database",
      "file": "apps/api/src/integrations/anilist/sync.ts",
      "line": 196,
      "title": "1 335 anime have an AniList id and still no synopsis after enrichment",
      "impact": "These are the rows the enricher can reach and did not fill. Whether that is AniList genuinely having no description, a run that has not covered them yet, or resolveFields refusing the write cannot be told apart from the outside, because metadata_runs records counts and not per-row outcomes. For a viewer it is an anime page with an empty description and no explanation.",
      "suggestedFix": "Record a per-row outcome for each enrichment attempt so \"no description upstream\" and \"never attempted\" stop looking identical.",
      "effort": "M",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0011",
      "severity": "MEDIUM",
      "category": "security",
      "file": "apps/api/src/modules/comments/routes.ts",
      "line": 146,
      "title": "Several user-facing write routes carry only the global rate limit",
      "impact": "WRITE_LIMIT is applied to eight routes; these are not among them, and 300 requests a minute is a flooding budget rather than a limit: POST /v1/comments/:id/like, PATCH /v1/forum/topics/:id, PUT and DELETE /v1/me/library/:animeId, DELETE /v1/me/favorites/:animeId, PATCH /v1/me/progress/:episodeId, PATCH /v1/profiles/me, PATCH and DELETE /v1/me/settings, POST and DELETE /v1/w2g. Vote manipulation and write amplification against the database are both cheap.",
      "suggestedFix": "Apply WRITE_LIMIT to every mutating route that an ordinary account can reach.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0012",
      "severity": "MEDIUM",
      "category": "database",
      "file": "database/migrations/0002_anime.sql",
      "line": 21,
      "title": "222 canonical titles are held by more than one anime row",
      "impact": "Duplicate catalogue entries split a show's episodes, its library entries and its comments across two ids, and the viewer sees the same title twice in search with different progress on each. Some are legitimate — AniList splits seasons that MyAnimeList keeps together — which is exactly why they need looking at rather than assuming.",
      "suggestedFix": "Cross-reference the duplicate titles against mapping_conflicts.held_by and merge the pairs that are one show.",
      "effort": "M",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0013",
      "severity": "MEDIUM",
      "category": "functional",
      "file": "apps/api/src/modules/analytics/routes.ts",
      "line": 232,
      "title": "The name /v1/admin/audit is already taken by the audit trail",
      "impact": "The audit-status report was specified to live at GET /v1/admin/audit, which is the audit log's route. Fastify refuses a duplicate route at startup, so registering the second one would have stopped the whole application from booting rather than failing at the request. The report is served from /v1/admin/audit/report instead, and the admin section key audit-log was freed so that /admin/audit could name the new page.",
      "suggestedFix": "Keep the two audits distinguishable by name — audit log and audit report — everywhere they are referred to.",
      "effort": "S",
      "status": "fixed"
    },
    {
      "id": "YUME-AUDIT-0014",
      "severity": "MEDIUM",
      "category": "frontend",
      "file": "apps/web/css/tokens.css",
      "line": 27,
      "title": "The palette has no yellow, so two severity levels share the amber hue",
      "impact": "Severity is meant to read critical red, high orange, medium yellow, low grey. The tokens offer red, amber and grey and nothing between amber and green, so high and medium are separated by lightness within one hue rather than by hue. The badges carry the word as well as the colour so nothing is conveyed by colour alone, but at badge size the two amber steps are harder to tell apart than the design intends.",
      "suggestedFix": "Add a yellow primitive to tokens.css and point --severity-medium at it, so the four levels differ by hue.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0015",
      "severity": "LOW",
      "category": "database",
      "file": "apps/api/src/modules/changelog/routes.ts",
      "line": 204,
      "title": "Release entries are inserted one statement at a time",
      "impact": "writeEntries() issues one INSERT per line of a release. It is inside a transaction and a release has a handful of lines, so the cost is small — but it is a round trip per row on a request path, and the shape scales with whatever somebody pastes in.",
      "suggestedFix": "Insert the entries in one statement with unnest.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0016",
      "severity": "LOW",
      "category": "database",
      "file": "apps/api/src/modules/library/achievements.ts",
      "line": 178,
      "title": "Achievement grants run two statements per achievement earned",
      "impact": "grantNew() loops over the newly earned achievements with an INSERT and then an XP insert inside the loop. Only newly earned ones are in the list, so this is normally zero or one iteration — but a founder import that crosses every threshold at once makes it the whole catalogue in one request.",
      "suggestedFix": "Grant the set in one INSERT ... SELECT and write the XP events from its RETURNING rows.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0017",
      "severity": "LOW",
      "category": "database",
      "file": "database/migrations/0002_anime.sql",
      "line": 201,
      "title": "512 anime hold no episode rows at all",
      "impact": "The anime page offers no episode list and the watch route has nothing to open. Some are legitimately unreleased, so this is a number to look at rather than a fault on its own — it is listed because nothing currently reports it.",
      "suggestedFix": "Surface the count in the metadata section so unreleased and un-imported stop looking the same.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0018",
      "severity": "LOW",
      "category": "frontend",
      "file": "apps/web/css/style.css",
      "line": 1062,
      "title": "Some controls are under 32 px tall at 375 px wide",
      "impact": "Measured in a real browser at 375x812: the library tabs are 30 px tall, the admin range buttons 24 px, and the footer links 16 to 21 px. All clear the 24x24 WCAG 2.5.8 minimum except the footer links, and all are below the 44 px that thumbs actually want. Nothing is unreachable; several things are fiddly.",
      "suggestedFix": "Raise the minimum hit area on tabs, range buttons and footer links to 44 px with padding rather than font size.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0019",
      "severity": "LOW",
      "category": "security",
      "file": "apps/api/src/modules/auth/routes.ts",
      "line": 236,
      "title": "POST /v1/auth/logout and /logout-all accept a body with no schema",
      "impact": "They are the only two write routes in the API whose body is read without a JSON schema. Both coerce safely — (request.body ?? {}) and a hash of whatever refreshToken holds — so there is nothing exploitable here today; it is the one gap in otherwise complete validation coverage.",
      "suggestedFix": "Give both a body schema with additionalProperties false.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0020",
      "severity": "LOW",
      "category": "security",
      "file": "database/migrations/0012_permissions_expansion.sql",
      "line": 262,
      "title": "The permissions table still lists the removed extension platform",
      "impact": "Fifteen extension_* permissions and the whole developer group are still rows in permissions, left behind when the platform was removed. They are marked planned and nothing checks them, so they grant nothing — but the admin role holds 359 permissions and a role editor that lists rights the product does not have makes the real ones harder to review.",
      "suggestedFix": "Delete the permission rows for subsystems that no longer exist, in the same migration style 0031 used for their tables.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0021",
      "severity": "LOW",
      "category": "database",
      "file": "database/migrations/0002_anime.sql",
      "line": 226,
      "title": "anime_relations is sequentially scanned 421 084 times",
      "impact": "The most scanned table on the instance, against 6 857 497 index scans on the same table. It holds 22 602 rows across three columns, so the planner choosing a scan is often the cheaper answer and this may be correct behaviour rather than a missing index — but it is the largest single scan count in pg_stat_user_tables and nothing has confirmed which of the two it is.",
      "suggestedFix": "EXPLAIN the relation lookups on the anime detail path and confirm the planner's choice before adding anything.",
      "effort": "S",
      "status": "open"
    },
    {
      "id": "YUME-AUDIT-0022",
      "severity": "LOW",
      "category": "functional",
      "file": "apps/web/test/achievements.test.mjs",
      "line": 1,
      "title": "One web test cannot run without the workspace being installed",
      "impact": "test/achievements.test.mjs reaches into apps/api and fails with ERR_MODULE_NOT_FOUND: Cannot find package '@yume/database' on a checkout where npm ci has not been run. The other 22 files in the suite do not, so the failure looks like a broken test rather than a missing install.",
      "suggestedFix": "Read the achievement catalogue from the server file as text, the way prefs.test.mjs pins the preference spec, instead of importing a module that opens a database connection.",
      "effort": "S",
      "status": "open"
    }
  ]
}
